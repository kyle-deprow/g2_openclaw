# Azure OpenAI API key for the G2 OpenClaw gateway.
#
# Retrieve with:
#   az cognitiveservices account keys list \
#     --name oai-ss-aisense-dev-eastus \
#     --resource-group rg-ss-aisense-dev-eastus \
#     --query key1 -o tsv
#
# Or find it in the Azure Portal under:
#   Cognitive Services → oai-ss-aisense-dev-eastus → Keys and Endpoint
AZURE_OPENAI_API_KEY=
