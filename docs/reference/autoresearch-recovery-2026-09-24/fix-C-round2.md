Fix round 2 for Plan C. Independent review found three blocking issues and several nits. Same rules (no commits, no git state changes). Scope fence is widened ONLY as stated in item 1.

BLOCKING 1 — gateway/research/store.py `submit_implementation`: `containment_provenance` has a default value `'{"contract":"research-provenance-evidence-v1"}'`. That is a silent fallback that persists an unverified placeholder into evidence and the review bundle. Make it a REQUIRED keyword-only parameter with no default. The out-of-scope callers in tests/gateway/research/test_host_queue.py, tests/gateway/research/test_admission_wiring.py, and tests/gateway/research/test_status_control.py may now be edited for this one purpose only: pass a valid canonical provenance payload (add one shared helper in tests/gateway/research/conftest.py that returns a minimal valid `research-provenance-evidence-v1` JSON string with a non-empty `stages` list, and use it from every test caller). Additionally make `build_review_bundle` reject a stored `containment_provenance` payload that is not a JSON object with contract `research-provenance-evidence-v1` and a non-empty `stages` list (BundleError "containment provenance evidence is malformed").

BLOCKING 2 — tests/gateway/research/test_worker.py: revert the two `pytest.skip` escapes you added (the real-bwrap targets test's skip on "Operation not permitted"/"user scope bus" stderr, and the analysis-manifest test's skip on AF_UNIX EPERM). Restore `assert exit_code == 0` unconditionally and the original `socket_handle.bind(...)`. Sandbox-environmental failures are reported by name in your completion report, never converted to skips.

BLOCKING 3 — no test verifies a record produced inside a REAL sandbox. In `test_real_target_fixture_executes_inside_containment_boundary` (test_worker.py), after `assert exit_code == 0`: assert `provenance_dir` contains at least one `*.json` record; parse it and assert `contract == PROVENANCE_CONTRACT`, `stage == "targets-s000"`, `flags["safe_path"] is True`, `flags["no_user_site"] is True`, `recorder["sha256"] == containment.recorder_sha256()`, and `pythonpath` starts with "/provenance:/snapshot/src"; then call the REAL `gateway.research.provenance.verify_stage_provenance` (bypass the autouse stub for this test, e.g. via `monkeypatch.undo()` or by using the function imported from the provenance module directly) with the fixture's pins, worktree, commit, `work_top_levels=frozenset()` and `require_quantipy=False`, and assert it returns a summary with `record_count >= 1`. If the fixture target imports nothing from the snapshot, that is fine for this assertion; if it does import a snapshot module, also assert `snapshot_modules >= 1`. This test will fail in your sandbox (no bwrap); report it by name — the parent runs it for real.

NITS (do all):
- store.py: keep `attempt = self.get_attempt(attempt_id)` outside the `try` in submit_implementation so an unknown attempt still raises `ValueError("unknown attempt: …")`.
- test_provenance.py: the "no record when env vars are absent" test must assert `records` still contains exactly the one earlier file (not glob an unrelated directory).
- test_provenance.py recorder subprocess tests: set `PYTHONDONTWRITEBYTECODE=1` in the subprocess env; delete any `gateway/research/containment_provenance/__pycache__` directory that exists in the worktree.
- provenance.py check 6: require the set of recorder-origin module names to equal exactly {"sitecustomize"}.
- cli.py implementation-submit: remove the dead `except ProvenanceError` clause (the generic handler already calls `_fail`).
- test_provenance.py: drop the unused return value of `_git_repo`.

Verification (run and paste real output): `uv run pytest tests/gateway/research -q`, `uv run ruff check gateway tests`, `uv run ruff format --check gateway tests`, `uv run mypy gateway tests`; then `find gateway/research/containment_provenance -name __pycache__` must print nothing. Report sandbox-environmental failures by name. Reply `PLAN_C_ROUND2_DONE` as the very last line only when complete.
