Fix round 3 for Plan C. The parent ran `test_real_target_fixture_executes_inside_containment_boundary` under real bubblewrap: the in-sandbox record IS written and matches contract/stage/recorder, but `flags["safe_path"] is True` fails. Root cause is a contract mismatch, not a recorder bug: the worker adds `-P -s` only to the evaluator and analysis commands it builds; the targets argv is authored by the implementer and passed through `validate_targets_argv`/`rewrite_targets_argv` unchanged (the real H0006-A001 argv was `<venv python> <worktree>/scripts/h0006_targets.py --panel …` with no `-P`).

Decision: interpreter-flag enforcement applies only to worker-built stages.
1. gateway/research/provenance.py `verify_stage_provenance`: add keyword `require_interpreter_flags: bool`. When True, require `flags.safe_path` and `flags.no_user_site` both True (as now). When False, still require the `flags` object to be present with both boolean keys, but do not require their values; include `"interpreter_flags": {"safe_path": …, "no_user_site": …}` (the observed values from the first record, and a failure if records disagree) in the returned summary in both modes.
2. worker.py: pass `require_interpreter_flags=False` for `targets-*` stages and `True` for `evaluate-*` and `analysis`. provenance.py `validate_provenance_evidence`: same rule by stage kind.
3. test_worker.py real-bwrap test: replace the `flags["safe_path"] is True` / `no_user_site is True` assertions with `isinstance(flags["safe_path"], bool)` and `isinstance(flags["no_user_site"], bool)`, keep every other assertion, and assert the returned summary's `interpreter_flags` matches the record. Add unit tests in test_provenance.py: a targets record with safe_path False passes with `require_interpreter_flags=False` and the summary reports it; the same record fails with `require_interpreter_flags=True`; an evaluate stage in `validate_provenance_evidence` with safe_path False is refused.
4. Skill docs (the three files you already edited): one sentence noting that evaluator and analysis stages run `python -P -s` and are verified for it, while target entrypoints are verified by module origin and hashes rather than interpreter flags.

Same rules: no commits, no git state changes, only files already in your scope. Verification: `uv run pytest tests/gateway/research -q`, `uv run ruff check gateway tests`, `uv run ruff format --check gateway tests`, `uv run mypy gateway tests`. Report sandbox-environmental failures by name. Reply `PLAN_C_ROUND3_DONE` as the very last line only when complete.
