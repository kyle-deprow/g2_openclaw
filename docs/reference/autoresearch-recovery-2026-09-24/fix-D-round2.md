Fix round 2 for Plan D. Independent review found one blocking issue and one nit to address. Same scope fence, same rules (no commits, no git state changes, only the four files you already own).

BLOCKING 1 — gateway/research/store.py `supersede_failed_review` drops and re-creates the `immutable_attempt_evidence` trigger at runtime. That silently disables a declared integrity control. The plan's "do not change the schema" line is hereby superseded by this decision: make the exception DECLARED and NARROW in `initialize()` instead.
- Redefine the trigger in `initialize()` so an UPDATE of attempt_evidence aborts UNLESS all of: NEW.attempt_id = OLD.attempt_id, NEW.kind = OLD.kind, OLD.kind IN ('review','review_host_evidence'), and a row with attempt_id = OLD.attempt_id and kind = OLD.kind || '_superseded' already exists (i.e. the old payload has been preserved first, in the same transaction). Keep every other UPDATE (any other kind, any key change) aborting exactly as today.
- Add a migration in `initialize()` for existing databases (follow the existing upgrade pattern used there, e.g. the `PRAGMA table_info(campaign)` check): read the trigger SQL from sqlite_master and if it is the old unconditional form, DROP and CREATE the new one once, inside `initialize()` only. Never touch triggers anywhere else.
- Remove the DROP/CREATE from `supersede_failed_review`; insert the two `*_superseded` rows BEFORE the two UPDATEs so the trigger condition holds.
- Tests (test_store.py): a direct UPDATE of a `review` row without a `review_superseded` row still aborts; an UPDATE of any other kind (e.g. `implementation`) aborts even when a `*_superseded` row exists; changing kind/attempt_id aborts; opening a store created with the OLD trigger text (create it by hand in a tmp DB, then call `initialize()`) migrates to the new trigger and `supersede_failed_review` works on it.

NIT 2 — store.py `current = replace(current, state=AttemptState.IMPLEMENTED)` launders the `submit_review` state guard. Either compute the four review fields directly without the fake state, or keep it with a short comment explaining that the explicit REVIEW_FAILED check above is the real guard. Your choice; say which.

NIT 3 — tests: add one FAIL→FAIL supersession case (synthetic FAIL with reason superseded by a transcript-verified FAIL with four findings), asserting the attempt state stays REVIEW_FAILED, the findings are the transcript's, and exactly one `review_recollected` event exists. This is the exact production case.

Verification (run and paste real output): `uv run pytest tests/gateway/research -q`, `uv run ruff check gateway tests`, `uv run ruff format --check gateway tests`, `uv run mypy gateway tests`. Sandbox-environmental containment failures may be reported by name. Reply `PLAN_D_ROUND2_DONE` as the very last line only when complete.
